# NIM instance + per-user API auth (headers X-NIM-API-USER / X-NIM-API-KEY).
# The API may run key-optional; if so, USER/KEY can be blank for reads.
NIM_URL=http://NIM_HOST
NIM_API_USER=
NIM_API_KEY=
